{
  "buildCommand": "npm run db:generate && npx prisma db push --skip-generate --accept-data-loss && npm run db:seed && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["bom1"],
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  },
  "crons": [
    {
      "path": "/api/cron/discover",
      "schedule": "30 3 * * *"
    }
  ]
}
